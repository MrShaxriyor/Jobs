Job Listing REST API

This project is a simple backend system built with FastAPI.
Users can view job listings and add new job posts.

Endpoints:

GET /jobs
→ Returns all job listings

POST /jobs
→ Creates a new job listing

Technologies Used:
- FastAPI
- SQLite
- Pydantic
- REST API
